spring.application.name=AiStock

# --- Existing Remote PostgreSQL Configuration (Commented out for later use) ---
#spring.datasource.url=jdbc:postgresql://kyochonchicken.asuscomm.com:5432/postgres
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.username=postgres
#spring.datasource.password=vine3458
#spring.sql.init.mode=never


# --- Local In-Memory H2 Configuration (PostgreSQL Mode) ---
# MODE=PostgreSQL: To support PostgreSQL-specific queries in the existing repository.
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Initialize schema and data
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# Console for checking DB (Access at /h2-console) (??? ?? ?? /h2-console)
# JDBC URL for console: jdbc:h2:mem:testdb
spring.h2.console.enabled=true



# Logging(???? ?? ?? -> ??? ??)
logging.level.org.springframework.jdbc=debug